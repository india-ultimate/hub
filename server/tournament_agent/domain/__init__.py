"""Tournament computation with no model and no network in sight.

Scheduling and schedule validation are ordinary deterministic code: given a
tournament they produce the same answer every time, and they are testable without
touching a provider.
"""
