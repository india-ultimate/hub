import terms from "../markdowns/terms.md";
import StyledMarkdown from "./StyledMarkdown";

const TermsAndConditions = () => {
  return <StyledMarkdown markdown={terms} />;
};

export default TermsAndConditions;
