import privacy from "../markdowns/privacy.md";
import StyledMarkdown from "./StyledMarkdown";

const PrivacyPolicy = () => {
  return <StyledMarkdown markdown={privacy} />;
};

export default PrivacyPolicy;
